import React from 'react';
import useFirestore from '../../hooks/useFirestore';

const InterfaceSquare = () => {
//   const { docs } = useFirestore('images');

  return (
    <div className="rect">
      
    </div>
  )
}

export default InterfaceSquare;